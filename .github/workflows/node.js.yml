# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
env:
  ## Sets environment variable
  API_KEY: XXXXXXXXXXXX
  DATABASE: stackoverflow_lite
  DATABASE_URL: mysql://beb4a8c5b514b0:e7f555d2@us-cdbr-east-06.cleardb.net/heroku_4e49e7e07457860?reconnect=true
  DB_HOST: us-cdbr-east-06.cleardb.net
  DB_PASSWORD: e7f555d2
  DB_USERNAME: beb4a8c5b514b0
  SESSION_SECRET: eewsldd
  TEST_DATABASE: heroku_4e49e7e07457860

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - run: npm ci
    - run: npm run build --if-present
    - run: npm test
    - name: Coveralls GitHub Action
      uses: coverallsapp/github-action@1.1.3
